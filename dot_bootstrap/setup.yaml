- name: Machine setup
  hosts: localhost
  become: true
  connection: local
  gather_facts: true

  tasks:
    - name: Get my user
      ansible.builtin.set_fact:
        remote_regular_user: "{{ ansible_env.SUDO_USER or ansible_user_id }}"

    - name: Install packages
      ansible.builtin.package:
        name:
          - git
          - vim
          - gh
          - zsh
          - fzf
          - tmux
          - neovim
          - unzip
          - python3-pip  # Install pip for AWS CLI
        state: present

    - name: Install AWS CLI
      ansible.builtin.command: "pip3 install awscli"

    - name: Change shell to zsh
      ansible.builtin.user:
        name: "{{ remote_regular_user }}"
        shell: /usr/bin/zsh

    - name: Change default shell to Zsh
      ansible.builtin.user:
        name: "{{ ansible_env.SUDO_USER or ansible_env.USER }}"
        shell: /bin/zsh
      when: "'/usr/local/bin/zsh' not in ansible_user_shell"

    - name: Reload Zsh configuration
      ansible.builtin.command: "exec zsh"
      when: "'/usr/local/bin/zsh' in ansible_user_shell"
